# Root-owned dashboard authentication settings.  No credential belongs in this
# file.  Create the hash artifact with scripts/migrate_dashboard_auth.py from an
# explicitly supplied legacy backup, or with your secret-management workflow.

DASHBOARD_AUTH_FILE=/etc/kripta/dashboard-auth.json
DASHBOARD_AUTH_REQUIRED=1
FINANCIAL_SERVER_BIND=127.0.0.1
