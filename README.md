# Gex Dashboard Live
Combination of a Discord Bot and a website.

Website to show the exposure of multiple option's greeks in heatmap tables, as well as a fourier transformation of spot price and ATM IV, and IB levels plotted together with the spot price and the max and mix greek's exposure. 

To format the code `black` is used (installed with `pip`).
